# Database config
database:
  driver: postgres
  url: postgresql://postgres:password@localhost:5432/minddemo?sslmode=disable&search_path=public

# Migrate database config
migrate:
  dir: file://migrate

# RSA keys for auth
rsa:
  size: 2048
  public: ./secure/rsa.pub
  private: ./secure/rsa
